# Copy to .env and fill in (never commit .env)
PORT=3500
LOG_LEVEL=info

# Shared secret with relay / gateway (RELAY_SIM_SERVICE_SECRET must match)
SIM_SERVICE_SECRET=

# WebSocket URL of the relay this sim-bot connects to (same region as games)
RELAY_WS_URL=wss://your-relay.example.com/ws
